package com.dompet.api.features.carrinho.domain;

// DOC: Enum para status do carrinho — controla se é possível editar ou não.
/** Status do carrinho: ABERTO (em edição) ou FECHADO (após checkout). */
public enum CartStatus { ABERTO, FECHADO }
