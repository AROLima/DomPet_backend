Coloque aqui os arquivos gerados por:

flutter build web --dart-define=BASE_URL=/

Diretório de origem (projeto Flutter): build/web
Copie TUDO (index.html, flutter.js, assets/, icons/, manifest.json, favicon, etc) para este diretório.

IMPORTANTE:
- Ajuste BASE_URL para "/" (relativo) usando --dart-define ou lógica automática.
- Se for usar hash strategy em rotas (para evitar 404 em proxies sem fallback), configure no Flutter conforme instruções no README principal.
- Este arquivo é apenas informativo e pode ser removido em produção.
